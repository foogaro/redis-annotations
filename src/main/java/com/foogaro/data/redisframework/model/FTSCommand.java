package com.foogaro.data.redisframework.model;

public enum FTSCommand {

    CREATE, DROPINDEX, ON, HASH, JSON, PREFIX, SCHEMA, AS, TEXT, TAG, NUMERIC, SORTABLE, CASESENSITIVE, SEARCH, AGGREGATE;

}
